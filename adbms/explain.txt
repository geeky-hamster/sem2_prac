Without Index:
Create Table Student:
CREATE TABLE Student(
	roll_no INT, 
	name VARCHAR(50),
	course_id INT
);

Create table Course:
CREATE TABLE Course (
    course_id INT PRIMARY KEY,
    course_name VARCHAR(50)
);

Query:
EXPLAIN 
SELECT * FROM Student 
WHERE roll_no = 101;

Output:
 

With Index:
Create Index:
CREATE INDEX idx_roll ON Student(roll_no);

Query:
EXPLAIN 
SELECT * FROM Student 
WHERE roll_no = 101;
Output:
 

Join Query Plan:
Query:
EXPLAIN
SELECT s.name, c.course_name
FROM Student s
JOIN Course c
ON s.course_id = c.course_id;
Output:
 
Order by with and without Index:
Without Index:
EXPLAIN SELECT * FROM Student ORDER BY roll_no;
Output:
 
Explain Analyze (Actual Runtime):
EXPLAIN ANALYZE SELECT * FROM Student WHERE roll_no = 101;
Output:
 
